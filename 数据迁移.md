数据库迁移
========

## 用户表

```
insert into auth_user (id, username, first_name, last_name,  email, password, is_staff, is_active, is_superuser,last_login, date_joined)  select * from guoku.auth_user;
```
- - - -

## 商品表
```
 core_entity | CREATE TABLE `core_entity` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `user_id` int(11) DEFAULT NULL,
  `entity_hash` varchar(32) NOT NULL,
  `brand` varchar(256) NOT NULL,
  `title` varchar(256) NOT NULL,
  `intro` longtext NOT NULL,
  `price` decimal(20,2) NOT NULL,
  `mark` int(11) NOT NULL,
  `chief_image` varchar(64) NOT NULL,
  `detail_images` longtext NOT NULL,
  `created_time` datetime NOT NULL,
  `updated_time` datetime NOT NULL,
  `status` int(11) NOT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `entity_hash` (`entity_hash`),
  KEY `core_entity_6340c63c` (`user_id`),
  KEY `core_entity_5a5255da` (`price`),
  KEY `core_entity_31f1de87` (`mark`),
  KEY `core_entity_41e5f0d8` (`created_time`),
  KEY `core_entity_b7cc06d7` (`updated_time`),
  CONSTRAINT `user_id_refs_id_6e1b21e7` FOREIGN KEY (`user_id`) REFERENCES `auth_user` (`id`)
) ENGINE=InnoDB AUTO_INCREMENT=1421476 DEFAULT CHARSET=utf8 |
```


```
insert into core_entity (id, user_id, entity_hash, brand, title, intro, price, mark, chief_image, detail_images, created_time, updated_time, status) select id, creator_id, entity_hash, brand, title, intro, price, mark, chief_image, detail_images, created_time, updated_time, weight from guoku.base_entity
```